
@{
    ViewBag.Title = "UnauthorizedOperation";

}


<div class="mn-vh-100 d-flex align-items-center mx-1350">
    <div class="container-fluid">
        <!-- Card -->
        <div class="card justify-content-center pt-5">
            <div class="row justify-content-center">
                <div class="col-xl-12 text-center">
                    <div class="mb-n130 position-relative">
                        <h1 class="mb-20 c2">Usuario no autorizado!</h1>
                        <p class="mxw-550">No tiene permiso para ver este directorio o página con las credenciales que proporcionó..</p>
                        <div class="mt-3">
                            @*<a href="../authentication/login.html" class="details-btn">
                                Log In Now
                                <img src="../../../assets/img/svg/left-arrow-c2.svg" alt="" class="svg">
                            </a>*@
                        </div>
                    </div>

                    <img src="../../../assets/img/media/not-auth.png" alt="">
                </div>
            </div>
        </div>
        <!-- End Card -->
    </div>
</div>


<!-- Content Header (Page header) -->
<!--<section class="content-header">
    <h1>
        Error, no tienes permiso
    </h1>
</section>-->
<!-- Main content -->
<!--<section class="content">
    <div class="error-page">
        <h2 class="headline text-red">Error</h2>
        <div class="error-content">
            <h3><i class="fa fa-warning text-red"></i>Sin autorización</h3>
            @if (ViewBag.operacion != "")
            {
                <p>Operación @ViewBag.operacion </p>
            }
            @if (ViewBag.modulo != "")
            {
                <p>Modulo @ViewBag.modulo</p>
            }
            @if (ViewBag.msjeErrorExcepcion != "")
            {
                <p>@ViewBag.msjeErrorExcepcion</p>
            }
        </div>
    </div>-->
    <!-- /.error-page -->
<!--</section>-->

